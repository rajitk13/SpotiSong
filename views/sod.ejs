<%- include('partials/header'); -%>
    <div class='padding-section'>
        <h1>Song of the day!</h1>

        <h3>
            <%=name%> - Song Preview
        </h3>
        <audio controls="controls">
            <source src="<%=url%>" type="audio/mpeg">
        </audio>


    </div>
    <div class='padding-section'>
        <h2>Select Playlist</h2>
        <form action='/playID' method="post">
            <table class="table">
                <tr>
                    <th>Playlist Name</th>
                    <th>Song ID</th>
                    <th>Select Playlist</th>
                </tr>
                <% playlistDisp.body.items.forEach(function (data){ %>
                    <tr>
                        <td>
                            <%=data.description %>
                        </td>
                        <td>
                            <%=data.id%>
                        </td>
                        <td>
                            <input type="radio" name="radVal" value="<%=data.id%>" />

                        </td>
                    </tr>
                    <% }); %>
            </table>
            <input type="hidden" name="songVal" value="<%=songuri%>" id="songVal" />
            <button type="submit" id="success" class="btn btn-primary playlist">Select Playlist - Confirm</button>
        </form>


    </div>


    <%- include('partials/footer'); -%>